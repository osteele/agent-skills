# References

Downloaded papers cited by the project live here. Keep source notes, manifests,
and indexes under version control. Treat PDFs as a replaceable reading cache and
ignore them with `references/**/*.pdf`.

Use the `download-research-references` skill to archive cited open-access PDFs
and update the notebook's `BIBLIOGRAPHY.md`.
